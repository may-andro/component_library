import 'package:flutter/material.dart';

class CLCheckBoxWidget extends StatelessWidget {
  const CLCheckBoxWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
