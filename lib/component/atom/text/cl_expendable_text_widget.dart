import 'package:flutter/material.dart';

class CLExpendableTextWidget extends StatelessWidget {
  const CLExpendableTextWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
