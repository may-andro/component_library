import 'package:flutter/material.dart';

class CLRichTextWidget extends StatelessWidget {
  const CLRichTextWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
