import 'package:flutter/material.dart';

class CLAutoSizingTextWidget extends StatelessWidget {
  const CLAutoSizingTextWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
