import 'package:flutter/material.dart';

class CLShapeWidget extends StatelessWidget {
  const CLShapeWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
